# The Rust target directory, which contains compiled output
# and anything else Cargo creates and manages.
/target
# File that MacOS uses with Finder. Never want to commit this.
.DS_Store
# Any SQLite databases that get created (and related files)
*.sqlite3*